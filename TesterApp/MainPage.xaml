<phone:PhoneApplicationPage
    x:Class="TesterApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <!--Pivot Control-->
        <phone:Pivot Title="MY APPLICATION">
            <!--Pivot item one-->
            <phone:PivotItem Header="login">
                <Grid>
                    <StackPanel>
                        <Button Content="login" Tap="Button_Tap"></Button>
                        <phone:WebBrowser x:Name="banana" Navigating="banana_Navigating" Height="453"></phone:WebBrowser>
                    </StackPanel>
                </Grid>
            </phone:PivotItem>

            
            <!--Pivot item two-->
            <phone:PivotItem Header="gettoken">
                <Grid>
                    <StackPanel>
                        <Button Content="go" x:Name="btnGetToken" Tap="btnGetToken_Tap"></Button>
                        <TextBlock x:Name="coop"></TextBlock>
                    </StackPanel>
                </Grid>
            </phone:PivotItem>


            <phone:PivotItem Header="APIs">
                <Grid>
                    <StackPanel>
                        <Button Content="go" x:Name="btngetuser" Tap="btngetuser_Tap"></Button>
                        <Button Content="getroutes" x:Name="btnGetRoutes" Tap="btnGetRoutes_Tap"></Button>
                        <TextBlock x:Name="coop2"></TextBlock>
                    </StackPanel>
                </Grid>
            </phone:PivotItem>
        </phone:Pivot>
    </Grid>
    
</phone:PhoneApplicationPage>